import 'package:flutter/material.dart';

class CoomingSoon extends StatelessWidget {
  const CoomingSoon({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Image.asset('assets/background/coomingsoon.jpg'),
      ),
    );
  }
}
